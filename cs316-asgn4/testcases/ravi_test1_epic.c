void main();
main() {
	int a, b, c;
	float d, e;

	// a = a>1?b:d;

	// a = -d + a;

	// a = (a < 1)?a:d;

	a = (a + b) + (a + b) + ((d + e) + (a + b));	
	// a = a-1.0;	
	// a = -2;
	// b = 3;
	// c = 4;
	// d = 1.0;
	// e = 2.0;
	// a = a*b+c/b-c;
	// a = a > 2?b+4:c;
	// a = ((a == 2)?(b+4):(c-2)) + a + b;
	// if (!(a > 2 || b < 5) && (a < 3 || b > 7)) {}

	// if (a < 2 || d > 2.0) {}

	// if ((!(a + 3)) < 4 || a+2 > b && d > e) {
	// 	a = 2;
	// }
	// {
	// 	int a;
	// }

	// if (a > 2 || b < 5 && a > 3 && !b < 3) {
	// 	a = 2;
	// }
	
	// {
	// 	a = 5;
	// }
	// if (a*b + c > 5)
	// 	a = 1;
	// else {
	// 	c = 2;
	// }
	// if (a <= b)
	// 	a = 2;
	// if (a > b)
	// 	a = 2;
	// else 
	// 	a = 4;
	// if (a > b){}
	// if (a >= b){}
	// if (a == b){}
	// if (a != b){}
	// a = a == 2 ? b + 3 : c - 2;
	// a = 0;

	// while (a > 4) {
	// 	a = a+1;
	// }
	// do {
	// 	a = a+1;
	// } while (a < 4);
}